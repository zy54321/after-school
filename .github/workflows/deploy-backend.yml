name: Deploy Backend to Tencent Cloud

on:
  push:
    branches: [ "main" ]
    paths:
      - "server/**"
      - ".github/workflows/deploy-backend.yml" # 建议加上这一行，这样修改 workflow 本身也能触发部署

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: 🚚 Checkout code
        uses: actions/checkout@v4

      - name: 🚀 Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USERNAME }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: 22
          script: |
            echo "1. 进入项目根目录..."
            cd /var/www/after-school

            echo "2. 强制同步最新代码..."
            # ⬇️ 核心修改：使用强制重置，彻底解决 git pull 失败问题
            git fetch --all
            git reset --hard origin/main

            echo "3. 进入后端目录并安装依赖..."
            cd server
            npm install --production

            echo "4. 重启 PM2 服务..."
            pm2 restart server --update-env
            
            echo "🎉 部署完成！"